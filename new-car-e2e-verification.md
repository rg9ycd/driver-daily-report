# 新車両画像・傷座標検証記録

実施日: 2026-08-28
対象: 共有版のHTTPSプレビュー

## 結果

クリーンなヘッドレスChromeで共通ログインを行い、添付された `car.webp` が読み込まれることを確認した。画像の実寸は **1447×2048**、Canvasの正規化描画サイズは **400×566** である。

Canvas上で傷マークを追加すると1件、同じ位置を再度クリックすると0件になった。PC相当の拡大操作後にマウスドラッグでパンし、パン後の位置へ傷マークを追加できた。Undoで0件、Redoで1件へ戻ることを確認した。検証時の倍率は130%、パン状態は `translate(15%, 5.22207%) scale(1.3)` で、座標変換とパン範囲制限が動作した。

印刷メディアでは表示中の帳票が **2ページ** となり、ズーム操作ボタンと操作ヒントは `display: none`、Canvasのstage変形は解除状態となった。

## 互換性対応

旧データの傷マークは、旧画像が400×220の表示領域に `object-fit: contain` されていたときの画像内相対位置を使い、新しい400×566の4面図全体へ移行する。新規保存データは座標バージョン2のラッパー形式で保存し、旧配列形式も読み込み時に自動移行する。

## 自動判定

全8項目に合格した。対象項目は、新画像URL、1447×2048の実寸、Canvas 400×566、傷追加・削除、拡大、パン後追加、Undo／Redo、印刷2ページと操作UI非表示である。

## PDFレイアウト追加確認

追加要件を反映したA4横2ページPDFを再生成した。表面では各運行記録の上段に小さい「（運転者）」「（同乗者）」が残り、日付と各記入欄は中央揃えになっている。裏面では「区分」列の実測幅が検査表全体の約17.5%となり、「点検内容」列を広く確保した。

車両4面図は右欄の利用可能な高さを基準に最大フィットさせ、上下2側面と正面・背面の全体が切れずに表示された。画像と傷マークは同じ1.08倍の基準スケールで重なり、操作UIは印刷時に非表示、表示帳票は2ページである。自動E2Eの追加判定12項目と、生成PDFの目視確認を完了した。

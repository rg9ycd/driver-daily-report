# 社員共通ログインと日報一覧の運用手順

## 日常の利用手順

社員は公開URLへアクセスし、会社から案内された**共通ID**と**当月の共通パスワード**でログインします。ログイン後は日報を入力して保存し、必要に応じて画面上部の「日報一覧」から過去データを参照します。一覧では日付範囲、車両番号、SQ、運転者、現場名で検索でき、日付・車両番号・SQ・運転者・更新日時の見出しを押すと昇順／降順を切り替えられます。

一覧の「開く」を押すと、その日報を入力・帳票プレビュー画面で再読込できます。修正後に「日報を保存」を押すと、同じ日報が更新されます。共有端末では、利用後に画面上部のログアウトボタンを押してください。

## 毎月のパスワード更新

月初にプロジェクト管理画面の**Settings → Secrets**で、`COMPANY_LOGIN_PASSWORD` を新しい共通パスワードへ更新します。同時に、`COMPANY_PASSWORD_VERSION` を年月形式などの新しい値へ更新してください。たとえば、2026年9月分なら `2026-09` を設定します。`COMPANY_LOGIN_ID` は変更する必要がなければ据え置きで構いません。

`COMPANY_PASSWORD_VERSION` を変更すると、それまでのログインCookieは無効になります。社員は次回操作時に新しいパスワードで再ログインする必要があります。パスワードだけを変更して世代値を変更しない場合、既存セッションが最長12時間残る可能性があるため、**パスワードと世代値は必ず同時に更新**してください。

## 管理上の注意

共通ID・パスワードは社内の承認済みチャネルでのみ配布し、社外へ転送しないでください。退職・外部委託終了などで認証情報の見直しが必要になった場合は、月次を待たずにパスワードと世代値を更新できます。保存済み日報は社内の共通台帳として扱われ、ログイン済みの社員は一覧から検索・閲覧できます。
